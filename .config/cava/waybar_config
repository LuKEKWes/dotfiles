[general]
bars = 8             # Exactly 8 bars as requested
framerate = 60

[input]
method = pulse
source = auto

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 7
bar_delimiter = 59
channels = mono      # 'mono' merges Left/Right (removes the mirror effect)
mono_option = average
